name: use environment variables
on: push


jobs:
  def-var:
    runs-on: ubuntu-latest
    env:
      PROJECT_NAME: "ZeroOps"
      AUTHOR: "taha mousnia"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Print environment variables
        run: |
          echo "$PROJECT_NAME by $AUTHOR"
    